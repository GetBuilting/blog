name: Deploy mini_mall

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests (placeholder)
        run: |
          echo "Tests passed"

      - name: Deploy
        run: |
          echo "Deploy step — configure your VPS/cloud deploy here"
          # Example: rsync to VPS
          # rsync -avz --exclude='instance/' --exclude='.git/' ./ user@host:/path/to/app/
          # ssh user@host 'sudo systemctl restart mini_mall'
